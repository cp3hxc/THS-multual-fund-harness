# 桌面应用验收记录

## 独立 PandaAI 风格策略 Agent（2026-09-21）

- 入口：`start-panda-strategy.command` → `/panda-strategy-agent.html`；与原基金工作台并行保留。
- 桌面验收通过：真实 Codex 模型、模型/思考强度、只读账户权限、会话恢复/置顶/重命名/归档、工具进度和中断。浏览器入口仅用于界面预览，会明确提示不运行模型。
- 五张策略卡均支持编辑名称、说明、受控参数和个人版本；回测显示阶段、进度、耗时、日志、曲线、交易位置与费用口径。
- 三个单基金策略使用扶摇复权净值；创业板策略使用复权净值与五年 PE 分位；行业轮动使用 Skill 固定的 23 只行业基金池和沪深300基准。两套专用策略已用真实数据完成回测。
- 页面已接入独立「我的计划」页面：保存前确认标的和资金安排，保存后可开启跟踪、暂停、归档和检查信号；活动计划启动时检查，运行期间每 30 分钟检查一次。
- 五个策略均保留实际执行的 Skill 版本、参数快照、数据日期和缺失原因；回测曲线标明规则模拟，不称为账户收益。实际计算版本为创业板 v0.3.21、行业轮动 v0.3.9。
- 自动化检查：Python 40 项通过；桌面 Agent 4 项通过；JavaScript 语法、Python AST、`git diff --check` 通过。

验收日期：2026-09-21（Asia/Shanghai）

## 自动化检查

- Python 业务与 MCP 测试：33 项通过。覆盖持仓响应、待确认资金与已确认持仓分离、账户简报聚合与批量日期、业务错误、策略/自选/待办、订单日期与游标分页、不透明账户引用、完整申购协议留痕、真实标量留痕编号响应、批量撤单部分失败及工具结果元数据。
- 桌面 Agent 运行层测试：4 项通过。覆盖账户工具授权隔离、模型与高推理强度固定、页面上下文与 skill 输入，以及澄清问题等待/续接协议。
- JavaScript 语法检查：`desktop/main.js`、`desktop/preload.js`、`desktop/agent-runtime.js` 和 `workbench.js` 通过。
- Electron arm64 目录包：构建通过。
- 金融写操作边界：MCP 只注册 22 个查询、准备或本地草稿工具。真实申购、支付、赎回与撤单不提供给 Agent；用户需在同花顺 App 完成实际交易，再由工作台核对订单。
- 交易防重：模拟 SDK 验证一次性确认令牌、支付账户锁定、订单号/交易账户锁定和撤单退款来源计算；本地校验或协议留痕失败不消耗令牌，真实申购请求前才原子锁定；批量撤单逐笔复核并返回独立结果。
- 本轮未执行任何真实申购、赎回或撤单；真实端到端调用需由用户用实际小额订单在最终确认页完成。

## 本机使用验收

- 应用可从 Finder 双击启动，主进程自动连接 Python 服务和 Codex App Server。
- 真实 thsfund 持仓在桌面窗口中成功加载；查询失败会保留为错误状态，不会显示成“没有持仓”。
- 「问 AI」新会话时保留页面标题、说明与快捷入口，Agent 对话卡片内只显示输入区；发起对话后再展开会话与执行过程。
- 「问 AI」页面会从同一份真实持仓快照生成本机账户晨报，展示基金资产、基金最新日收益率、持有收益、钱包份额和涨跌贡献摘要。日收益率按“日收益金额 ÷ 日初已确认基金资产”估算，不包含待确认资金与钱包；该摘要不自动发送给模型，深入解读仍经过现有会话账户数据授权。
- 申购确认页不再展示协议列表、协议勾选框或手工输入「确认申购」；用户选择支付方式后，一次点击「确认申购」，服务端先记录该基金返回的全部协议，再携带留痕编号提交申购。协议缺失或无法解析时会在确认前停止。
- 每条真实持仓均显示「申购 / 赎回 / 详情」入口，申购准备可成功读取 thsfund 真实规则与费率。
- 组合分析将份额为「待确认」的金额单列为待确认资金；基金资产总额保留真实账户口径，历史收益、波动、回撤和相关性只使用已确认持仓。资产配置在窄窗口中不会再溢出卡片。
- 「订单中心」不再显示顶部申购、赎回或按号撤单按钮；列表显示全选、行选择及批量撤单状态。本次真实处理中订单均不可撤，因此复选框按真实资格禁用。
- 本机 Codex 订阅登录可复用，桌面端可显示订阅连接状态。
- 订阅设置从 Codex App Server 实时读取可用模型及其推理强度；选择仅作用于之后新建的会话，Agent 面板显示每个会话固定的模型和推理强度。
- 安装版实测模型下拉列出 `GPT-6-Astra`、`GPT-5.6-Sol`、`GPT-5.6-Terra`、`GPT-5.6-Luna` 和 `GPT-5.5`；切换模型会同步刷新可用推理强度。保存后重启应用仍恢复所选设置，未保存的临时选择不会污染配置。
- 安装版订阅通道完成无账户数据的真实调用，运行记录确认模型为 `gpt-6-astra`、推理强度为 `high`，最终返回“连接正常”；连接期间 Codex 自动重试后恢复。
- Agent 支持 Codex 澄清问题表单、执行计划、可读推理摘要与安全 Markdown/表格渲染；不会再对澄清请求自动返回空答案。
- 真实账户简报工具实测成功：一次查询返回 12 只已确认持仓，12 只均取得净值或收益日期，未出现持仓类别查询失败。
- 非账户 Agent 验收：在未勾选账户授权的会话中，订阅模式成功调用 `list_strategy_templates`，面板显示工具开始、完成和续接回答，结果为 5 个模板。首次请求曾因订阅 WebSocket 连接超时自动重试，最终完成。
- 会话恢复：退出并重新打开安装版后，已完成的用户消息、工具卡片和 Agent 回答均正常恢复。对 Codex 已丢失的旧会话索引，应用会自动清理并继续加载业务页。
- 真实账户数据未在自动验收中发送给模型；该操作需由用户在对应会话勾选授权。
- 第三方 Responses API：未验证，需要用户保存实际服务的 Base URL、模型 ID 和 API Key 后运行内置兼容性检测。

## 安装产物

- 项目构建产物：`dist/mac-arm64/基金 AI 工作台.app`
- 固定入口：`/Applications/基金 AI 工作台.app`
- 应用为本机 arm64 开发包，未签名、未公证，不作为其他 Mac 的通用发行包。
- 构建脚本可复用本机已经校验的 Electron 缓存，在下载连接重置时仍能完成本机构建与安装。
